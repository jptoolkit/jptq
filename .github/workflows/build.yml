name: Build

on:
    push:
        branches:
            - master
        tags:
            - 'v*'

    pull_request:

jobs:
    tests:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v6
            - name: Run tests
              run: ./tests/test_jptq.sh
